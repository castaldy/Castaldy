#include <iostream>
using namespace std;

int main(){
    double a, b;
    cout<<"entrer la premiere valeur\n"; //évite les accents , l'affichage est pas très beau ! ;) Ce site ne gère pas les accents
    cin>>a;  // en C++ il faut finir toutes les instructions sauf la ligne de if et else par une virgule;
    cout<<"entrer la deuxieme valeur\n"; // met souvent le \n pour un plus bel affichage ! sur plusieurs lignes ... et non tout sur une même ligne...
    cin>>b;
    cout<<"la moyenne arithmetique des nombres est: "<<(a+b)/2;
}

