#include <iostream>

using namespace std;

int  main ()
{
	float a, b, c, tempb, tempc;
	
	cout << "entrer le premier nombre a: " << endl;
	cin >> a;
	cout << "entrer le second nombre b: " << endl; 
    cin >> b;
    cout << "entrer le second nombre bçc: " << endl; 
    cin >> c;
    
    cout << " Avant inversion, on a: a = " << a << ", b = " << b <<", c = " << c << endl ;
    
    tempc = c;    // on garde dans tempc, la valeur de c avant de le remplacer par a
    c = a;       // c est désormais a
    tempb = b;   //   on garde dans tempb, la valeur de b avant de le remplacer par c
    b = tempc;   // b devient c
    a = tempb;  // a devient b
    
    cout << " Apres inversion, on a: a = " << a << ", b = " << b <<", c = " << c << endl ;

}
