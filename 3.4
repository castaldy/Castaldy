#include <iostream>
#include <math.h>

using namespace std;

int main ()
{
	int x, resultat ;
	
	cout << "entrer le nombre x: " << endl;
	cin >> x;
	
	resultat = 3 * (pow(x, 6)) - 6* (pow(x, 2)) - 7;
    cout << "le resultat est "  << resultat << " pour x = " << x << endl;
}
