#include <stdio.h>
int main()
{
        int N;
        scanf("%d", &N);
        N=3608; // pas necessaire scanf en C c'est comme cin en C++
        printf("min= %d", N/60);
        printf("sek= %d", N%60);
 
   return 0;
}
